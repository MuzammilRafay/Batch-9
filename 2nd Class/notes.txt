Cascading Style Sheets = css
we will use id attribute for unique element
we will use class attribute for multiple elements

# = id
. = class

we will use id for specific element.
we will use class for multiple elements.

div is like a box (and it is used for structuring the layout)